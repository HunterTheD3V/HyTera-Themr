var jsdelivr = require('./index');
var util = require('util');

jsdelivr.url('jquery.tinymce@3.5.7', function(err, packages) {
    console.log(util.inspect(packages, {
        depth: null,
        colors: true
    }));
});
